<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Página de Inicio</title>
    <!-- Incluir el CSS de Bootstrap 5.3.3 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Incluir el CSS personalizado -->
    <link href="/src/css/base.css" rel="stylesheet" />
    <link href="/src/css/index.css" rel="stylesheet" />
  </head>
  <body>
    <!-- Incluir el navbar -->
    <%- include('partials/navbar') %>
    <audio id="audio" class="d-none" src="audio.mp3" type="audio/mpeg"></audio>
    <div class="main-content d-flex justify-content-center align-items-start flex-column gap-4">
      <!-- Tarjeta para el usuario -->
      <div class="card custom-card user-card">
        <div class="card-body text-center">
          <h2 class="card-title">Hola, <%= usuario.username %>!</h2>
        </div>
        <a class="m-auto" href="/album"><button class="btn btn-primary">Ver album</button></a>
      </div>

      <!-- Tarjeta para las tiradas -->
      <div class="card custom-card tiradas-card">
        <div class="card-body text-center">
          <!-- Incluir el partial de notificaciones -->
          <%- include('partials/notification', { message: message, type: type }) %>

          <h4 class="card-title">Tiradas disponibles: <strong><%= usuario.tiradas %></strong></h4>
          <div class="tiradas-buttons">
            <% if (usuario.tiradas >= 1) { %>
            <button class="btn btn-primary">Tirar 1</button>
            <% } %> <% if (usuario.tiradas >= 5) { %>
            <button class="btn btn-primary">Tirar 5</button>
            <% } %> <% if (usuario.tiradas >= 10) { %>
            <button class="btn btn-primary">Tirar 10</button>
            <% } %>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal para mostrar las cartas ganadas -->
    <div class="modal fade" id="resultModal" tabindex="-1" aria-labelledby="resultModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="resultModalLabel">Resultados de la Tirada</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body d-flex flex-wrap justify-content-center gap-3">
            <!-- Aquí se cargarán las tarjetas con los resultados -->
          </div>
        </div>
      </div>
    </div>

    <!-- Incluir el footer -->
    <%- include('partials/footer') %>

    <!-- Incluir el JavaScript de jQuery (opcional) -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" crossorigin="anonymous"></script>
    <!-- Incluir el JavaScript de Bootstrap 5.3.3 -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Incluir el JavaScript personalizado -->
    <script src="/src/js/base.js"></script>
    <script src="/src/js/index.js"></script>
  </body>
</html>
